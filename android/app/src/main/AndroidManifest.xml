<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- OpenCalc permissions — minimal set. See PRIVACY.md for details. -->
    <!-- VIBRATE: haptic feedback on calculator key taps. User can disable in Settings. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- INTERNET + ACCESS_NETWORK_STATE: required by Android WebView for PWA hybrid mode. -->
    <!-- The app works fully offline once installed; these are only for the WebView shell. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- OpenCalc does NOT request: CAMERA, RECORD_AUDIO, ACCESS_FINE_LOCATION,
         READ_CONTACTS, READ_EXTERNAL_STORAGE, WRITE_EXTERNAL_STORAGE,
         READ_PHONE_STATE, SEND_SMS, or any other sensitive permission.
         See PRIVACY.md for the full permissions table. -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoSplash"
            android:screenOrientation="user"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <!-- Declare that OpenCalc works offline and doesn't require touch -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="true" />
    <uses-feature android:name="android.hardware.vibrate" android:required="false" />
</manifest>
